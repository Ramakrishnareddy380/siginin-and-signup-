Pipeline Script: 
pipeline {
  agent any
  tools {nodejs "NodeJS}
  stages {
    stage('Build') {
      steps {
        git 'https://github.com/patebija/simple-no...
        bat 'npm install'
      }
    }
  }
}
Pipeline Script: 
pipeline {
  agent any
  tools {nodejs "node"}
  stages {
    stage('Build') {
      steps {
        git 'https://github.com/patebija/simple-no...
        bat 'npm install'
      }
    }
  }
}
